# Two-Device Delivery Evidence

Status: manual two-phone LAN NSD/TCP over local Wi-Fi prototype smoke completed; fresh
repeatable post-cleanup chat capture collected; separate BLE direct-route
evidence captured; June 7 connected-phone open-chat UI evidence captured.

Dates of raw evidence: 2026-06-01 historical manual LAN NSD/TCP over local Wi-Fi run;
2026-06-06 fresh repeatable post-cleanup LAN NSD/TCP over local Wi-Fi capture;
2026-06-06 BLE direct-route capture; 2026-06-07 connected-phone open-chat
capture.

Documentation refreshed: 2026-06-07.

Purpose: capture proof that the Android research prototype can exchange
messages between two QR-trusted devices over the local Android prototype
transport path and reach receipt-level UI states.

## Device Evidence

| Role | Device | Evidence |
| --- | --- | --- |
| Device A | Device A `ANDROID_DEVICE_A` | `artifacts/two-phone-test/2026-06-01/after-peer-fail-fanout-fix/ANDROID_DEVICE_A-final-ui.xml` |
| Device B | Device B `ANDROID_DEVICE_B` | `artifacts/two-phone-test/2026-06-01/after-peer-fail-fanout-fix/ANDROID_DEVICE_B-final-ui.xml` |

## Fresh Repeatable Capture

Current post-cleanup capture:

```text
artifacts/two-phone-test/repeatable-20260606-195249-post-cleanup-chat-delivery/
```

This run was generated by `scripts/capture_two_phone_smoke_evidence.sh` on
2026-06-06 at `2026-06-06T16:52:50Z` (`19:52:50` Europe/Moscow). It records:

- APK path:
  `app-android/app/build/outputs/apk/debug/app-debug.apk`;
- APK SHA-256:
  `88f371f68b8229d79bfdae2d596da63d541ba8fdeee5a12ef4c20c2d30e600dc`;
  this is the capture-time APK hash and should not be replaced by a later local
  rebuild hash unless a fresh two-phone capture is taken;
- git branch/SHA/source state:
  `android-research-panel-report-viewer` / `e542ea0` / `dirty`;
- Device A `ANDROID_DEVICE_A` screenshot and UI XML:
  `device-a-ANDROID_DEVICE_A/screen.png`, `device-a-ANDROID_DEVICE_A/ui.xml`;
- Device B `ANDROID_DEVICE_B` screenshot and UI XML:
  `device-b-ANDROID_DEVICE_B/screen.png`, `device-b-ANDROID_DEVICE_B/ui.xml`.

Visible state in the fresh capture:

- Device A is in an open chat with `Device B`; it shows received message
  `мгм8ощмощм` at `03:37` and multiple locally sent messages with
  `доставлено`.
- Device B is in an open chat with `Peer`; it shows sent message
  `мгм8ощмощм` at `03:37` with `доставлено`, received peer messages, and one
  failed diagnostic send `оаощсщосо` with `ошибка`.

Claim boundary for the fresh bundle: it is repeatable capture evidence for the
manual UI-visible two-phone prototype state. It does not automate message
sending, does not export authoritative in-app packet counters and does not prove
production security.

## Fresh June 7 Connected-Phone Capture

After both phones were reconnected, additional ADB bundles were captured on
2026-06-07. They are useful for provenance, environment status and current UI
state. They strengthen the manual UI evidence, but they do not replace the need
for an orchestrated fresh-send/counter run.

First bundle:

```text
artifacts/two-phone-test/repeatable-20260607-200131-fresh-20260607-connected-phones/
```

This bundle records two ADB-visible devices and APK/Git metadata, but it is not
Kraken app evidence: Device A `ANDROID_DEVICE_A` was on an unrelated screen and Device B
`ANDROID_DEVICE_B` was on Android Developer Settings.

Second bundle:

```text
artifacts/two-phone-test/repeatable-20260607-200227-fresh-20260607-kraken-opened/
```

This bundle records:

- generated at `2026-06-07T17:02:28Z` (`20:02:28` Europe/Moscow);
- git branch/SHA/source state:
  `android-research-panel-report-viewer` / `e542ea0` / `dirty`;
- APK SHA-256:
  `af1ab1e3bc333cbd93fc0fb0ec8336c7052bbbd30a4456af2195cd64442eb0c4`;
- Device A `ANDROID_DEVICE_A` and Device B `ANDROID_DEVICE_B` screenshots/UI XML showing the
  Kraken welcome screen.

Third bundle:

```text
artifacts/two-phone-test/repeatable-20260607-201107-fresh-20260607-after-open/
```

This bundle shows both phones on the Overview screen after opening Kraken:

- both devices have one active contact;
- Device A shows nearby transport still starting;
- Device B shows Bluetooth active, Wi-Fi off and last error `ble-connection:19`.

Fourth bundle:

```text
artifacts/two-phone-test/repeatable-20260607-201221-fresh-20260607-chats-tab/
```

This bundle shows both chat lists:

- Device A shows chat `Device B` with message `спвпрпо` at `22:44`;
- Device B shows chat `Peer` with `Вы: спвпрпо`, timestamp `22:44` and
  `доставлено`.

Fifth bundle:

```text
artifacts/two-phone-test/repeatable-20260607-201329-fresh-20260607-open-chat/
```

This bundle records:

- generated at `2026-06-07T17:13:29Z` (`20:13:29` Europe/Moscow);
- APK SHA-256:
  `af1ab1e3bc333cbd93fc0fb0ec8336c7052bbbd30a4456af2195cd64442eb0c4`;
- both open chat headers show `Bluetooth напрямую`;
- Device A shows peer `Device B`, incoming message `спвпрпо` at `22:44`, older
  incoming messages and multiple locally sent messages marked `доставлено`;
- Device B shows peer `Peer`, sent message `спвпрпо` at `22:44` marked
  `доставлено`, older delivered messages and some failed diagnostic messages
  marked `ошибка`.

Claim boundary for the June 7 bundles: they prove that both phones were
ADB-visible, Kraken opened on both devices, and the current APK can display a
Bluetooth-direct chat with delivered/error UI states. They do not prove an
automated fresh send during the capture, authoritative packet counters,
latency/loss/retry history, broad BLE reliability or production security.

Network diagnostics captured during the same investigation show both devices on
the local `NCC1` Wi-Fi/LAN segment:

| Device | Local IP evidence |
| --- | --- |
| Device A `ANDROID_DEVICE_A` | `192.168.1.x` in `artifacts/two-phone-test/2026-06-01/lan-debug-now/ANDROID_DEVICE_A-debug.txt` |
| Device B `ANDROID_DEVICE_B` | `192.168.1.y` in `artifacts/two-phone-test/2026-06-01/lan-debug-now/ANDROID_DEVICE_B-debug.txt` |

## Observed User-Visible State

Device A final UI evidence contains:

```text
Device B
ACTIVE
привет
доставлено пиру
Медвед
Приветик
```

Device B final UI evidence contains:

```text
Peer
ACTIVE
Приветик
доставлено пиру
превед
отправлено в транспорт
Медвед
```

This supersedes the older May 30 one-device reports that said completed
two-device delivery was still pending.

## Scenario Matrix

| Scenario | Expected result | Observed |
| --- | --- | --- |
| Two devices available | Device A and Device B are present in the raw evidence set | complete |
| Same local Wi-Fi/LAN segment | Both devices have `192.168.1.x` addresses on `NCC1` | complete |
| QR/relationship gate | Chat screen shows one `ACTIVE` relationship on both devices | complete |
| A sends message to B | Peer-visible message appears in final UI evidence | complete |
| B sends message to A | Peer-visible message appears in final UI evidence | complete |
| Delivery/receipt-level UI state | Both devices show `доставлено пиру` for delivered messages | complete |
| Production encryption | Not expected in this prototype | not implemented |
| Repeatable capture bundle | APK/hash, git state, device IDs, screenshots and UI XML captured after cleanup | complete for capture; message automation still needed |
| Latency/packet counters | Not captured as authoritative metrics in this report | still needed |
| BLE direct-route delivery | Covered separately by Device A/Device B BLE evidence and 2026-06-08 route-specific counters | `reports/out/ble_two_device_delivery_evidence_2026-06-06.md`, `reports/out/two_device_route_specific_smoke_2026-06-08.md/json` |
| Wi-Fi Direct delivery | Implemented as prototype transport; delivery evidence pending | Wi-Fi Direct readiness/registration/discovery-start diagnostics exist, but peer discovery and selected-route delivery are still open |

## Automated Evidence Around The Same Pipeline

The manual run is supported by unit/source tests recorded in
`reports/out/mesh_delivery_simulation.md`:

- two-node message delivery;
- receipt packet generation and processing;
- sender delivered-state update after receipt;
- queued message retry;
- queued receipt retry;
- unknown/pending/blocked/wrong-recipient rejection;
- duplicate packet rejection;
- malformed payload rejection;
- simulated store-and-forward relay with real relay disabled by default.

## Claim Boundary

Allowed claim:

> Kraken has manual two-phone Android prototype evidence for QR-gated local
> message exchange and receipt-level delivered UI state over the local
> LAN NSD/TCP over local Wi-Fi prototype path, plus separate manual evidence for a BLE direct route
> label with delivered chat messages.

Not allowed:

- completed production-secure messenger;
- production encryption;
- audited packet signatures;
- Android Keystore-backed non-exportable identity;
- BLE reliability/latency claims beyond the captured manual direct-route smoke;
- completed Wi-Fi Direct route/delivery evidence;
- latency or reliability metrics beyond this manual smoke.

## Remaining Work

1. Add automated message-send/receipt orchestration if the evidence needs to be
   repeatable without manual screen setup.
2. Run the repeatable route benchmark runner to move beyond current LAN/BLE
   smoke samples into median/p95/loss/retry evidence.
3. Add physical hostile/rejection cases for LAN/BLE/Wi-Fi Direct; current
   local hostile probes are debug-only and not radio-path injection.
4. Keep production crypto claims blocked until real crypto and Keystore work are
   implemented and reviewed.
